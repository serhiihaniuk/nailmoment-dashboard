This project uses the Next.js App Router from the root `app/` directory.

This folder is intentionally present so Next.js does not treat `src/pages/`
as a Pages Router directory. The `src/pages/` tree is the Feature-Sliced
Design pages layer, not Next.js routing.
